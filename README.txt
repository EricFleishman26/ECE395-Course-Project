Nihith:

- The makefile is being used for testing specific test cases, not compiling. Compiling with 'make' command will give errors. Compile with: g++ -std=c++11 -g *.cpp then run the executable.

- Eric